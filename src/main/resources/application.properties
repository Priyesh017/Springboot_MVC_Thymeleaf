# ==========================================
# POSTGRESQL CONFIGURATION
# Values are loaded from .env (local dev) or
# OS environment variables (Render production)
# ==========================================
app.datasource.postgres.url=${DB_URL}
app.datasource.postgres.username=${DB_USERNAME}
app.datasource.postgres.password=${DB_PASSWORD}